streamlit
opencv-python-headless
numpy
Pillow
pillow-heif
requests
beautifulsoup4
streamlit-cropper
# Nur fuer den lokalen exe-Build (build_exe.bat) noetig, nicht fuer Streamlit Cloud:
pyinstaller; sys_platform == "win32"

# OCR - rein per pip, keine Programm-/PATH-Installation noetig:
rapidocr-onnxruntime

# Optionale OCR-Fallbacks:
winocr; sys_platform == "win32"
pytesseract
